<!DOCTYPE html>

<title>CondicionEducativaAlumnos</title>

{% block body %}
    <h1>CondicionEducativaAlumnos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ condicion_educativa_alumno.id }}</td>
            </tr>
            <tr>
                <th>Ccts</th>
                <td>{{ condicion_educativa_alumno.ccts }}</td>
            </tr>
            <tr>
                <th>Numalumnas</th>
                <td>{{ condicion_educativa_alumno.numalumnas }}</td>
            </tr>
            <tr>
                <th>Numalumnos</th>
                <td>{{ condicion_educativa_alumno.numalumnos }}</td>
            </tr>
            <tr>
                <th>Grado</th>
                <td>{{ condicion_educativa_alumno.grado }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('condicion_educativa_alumnos_index') }}">back to list</a>

    <a href="{{ path('condicion_educativa_alumnos_edit', {'id': condicion_educativa_alumno.id}) }}">edit</a>

    {{ include('condicion_educativa_alumnos/_delete_form.html.twig') }}
{% endblock %}

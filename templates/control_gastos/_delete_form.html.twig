<form method="post" action="{{ path('control_gastos_delete', {'id': control_gasto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ control_gasto.id) }}">
    <button class="btn">Delete</button>
</form>

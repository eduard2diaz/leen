<form method="post" action="{{ path('rendicion_cuentas_delete', {'id': rendicion_cuenta.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rendicion_cuenta.id) }}">
    <button class="btn">Delete</button>
</form>

<form method="post" action="{{ path('codigo_postal_delete', {'id': codigo_postal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ codigo_postal.id) }}">
    <button class="btn">Delete</button>
</form>

{{ form_start(form,{'attr':{
    'id': form_id|default('tipo_condicion_new'),
    'class': 'm-form m-form--state'
}}) }}
<div class="modal-body" id="modal-body">
    <div class="row">
        <div class="col-md-12">
            <div class="form-group m-form__group">
                <label>
                    {{ form_label(form.condicion) }}
                </label>
                <div>
                    {{ form_widget(form.condicion) }}
                    {{ form_errors(form.condicion) }}
                </div>
            </div>
        </div>
    </div>
</div>
<div class="modal-footer">
    <a class="btn btn-sm btn-secondary" data-dismiss="modal"><i class="flaticon-cancel"></i> Cancelar</a>
    {% if eliminable is defined%}
        <a class="btn btn-danger btn-sm  eliminar_tipo_condicion"
           data-href="{{ path('tipo_condicion_delete', { 'id': tipocondicion.id }) }}"
           data-csrf="{{ csrf_token('delete' ~ tipocondicion.id) }}">
            <i class="flaticon-delete-1"></i> Eliminar</a>
    {% endif %}
    <button class="btn btn-sm btn-primary mt-ladda-btn ladda-button" data-style="expand-right" type="submit">
        <i class="flaticon-edit"></i>
        <span class="ladda-label">{{ action |default('Guardar') }}</span>
        <span class="ladda-spinner"></span>
    </button>
</div>
{{ form_end(form) }}
